import React, {Component} from 'react'
import {
    WriterWrapper
} from '../homeStyle'
class Writer extends Component {
    render() {
        return (
            <WriterWrapper>

            </WriterWrapper>
        )
    }
}

export default Writer